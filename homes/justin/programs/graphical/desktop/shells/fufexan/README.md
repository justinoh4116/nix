# quickshell config

To get this config working, run this command in this directory:

```sh
ln -s $(pwd) ~/.config/quickshell
```

Do not disturb can be controlled from the command line with:

```sh
quickshell-dnd toggle
quickshell-dnd on
quickshell-dnd off
quickshell-dnd status
```
